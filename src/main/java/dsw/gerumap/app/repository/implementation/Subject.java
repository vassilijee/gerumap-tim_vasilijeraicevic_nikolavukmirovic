package dsw.gerumap.app.repository.implementation;

import dsw.gerumap.app.repository.composite.MapNode;

public class Subject extends Element{
    public Subject(String name, MapNode parent) {
        super(name, parent);
    }
}
